<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

    <import resource="spring-tools.xml"/>
    <!--<import resource="spring-security.xml"/>-->
    <import resource="spring-social.xml"/>

    <!-- annotation-config достаточно определить только в parent-контексте -->
    <context:annotation-config/>

    <!-- Сканируем все, кроме controllers -->
    <context:component-scan base-package="org.verygroup.verybook">
        <context:exclude-filter type="regex" expression="org\.verygroup\.verybook\.web\..*"/>
        <context:exclude-filter type="regex" expression="org\.verygroup\.verybook\.rest\..*"/>
    </context:component-scan>
    <context:component-scan base-package="ru.javawebinar.**.service"/>

</beans>